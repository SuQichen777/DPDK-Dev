# SPDX-License-Identifier: BSD-3-Clause

sources = files(
        'main.c',
        'election.c',
        'networking.c',
        'timeout.c',
)

inc = include_directories('.', 'include')
deps += ['timer', 'ethdev', 'mbuf', 'mempool', 'eal']
